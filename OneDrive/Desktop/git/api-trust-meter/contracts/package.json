{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for API Trust Meter",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.22.1"
  },
  "dependencies": {
    "ethers": "^6.8.1"
  }
}